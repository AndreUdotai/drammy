extends layout

block content 
    h1= title

    ul
        each artist in artist_list
            li
                a(hreg=artist.url) #{artist.name}

        else
            li There are no artists.